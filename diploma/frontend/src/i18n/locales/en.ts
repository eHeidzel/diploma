import { IAppTranslations } from "../interfaces/index";
import { dashboard } from "../translations/en/dashboard.translations";
import { login } from "../translations/en/login.translations";
import { register } from "../translations/en/register.translations";
import { schedule } from "../translations/en/schedule.translations";
import { subjects } from "../translations/en/subjects.translations";
import { test } from "../translations/en/test.translations";
import { adminActivities } from "../translations/en/admin-activities.translations";
import { adminUsers } from "../translations/en/admin-users.translations";
import { adminSchedule } from "../translations/en/admin-schedule.translations";
import { learning } from "../translations/en/learning.translations";
import { profile } from "../translations/en/profile.translations";
import { settings } from "../translations/en/settings.translations";
import { workload } from "../translations/en/workload.translations";
import { myStudents } from "../translations/en/my-students.translations";
import { scheduleView } from "../translations/en/schedule-view.translations";
import { common } from "../translations/en/common.translations";

export const en = {
  dashboard: dashboard,
  login: login,
  register: register,
  schedule: schedule,
  subjects: subjects,
  test: test,
  adminActivities: adminActivities,
  adminUsers: adminUsers,
  adminSchedule: adminSchedule,
  learning: learning,
  profile: profile,
  settings: settings,
  workload: workload,
  myStudents: myStudents,
  scheduleView: scheduleView,
  common: common,
};